package com.attendance.system.enums;

public enum SessionStatus {
    CREATED, // Session has been created but not started
    STARTED, // Session is active and has been started
    STOPPED, // Session has been stopped
}